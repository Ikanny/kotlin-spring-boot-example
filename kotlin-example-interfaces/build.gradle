apply plugin: 'kotlin-spring'
apply plugin: 'org.springframework.boot'

dependencies {
    compile project(":kotlin-example-domain")

    // dependencies for spring boot
//    compile "org.springframework.boot:spring-boot-starter-security"
}